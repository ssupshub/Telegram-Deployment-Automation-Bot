# Development and testing dependencies — all pinned for reproducible CI.
# Update with: pip-compile requirements-dev.in
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-mock==3.14.0
ruff==0.8.6
detect-secrets==1.5.0
